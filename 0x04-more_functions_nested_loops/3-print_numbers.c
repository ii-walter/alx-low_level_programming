#include "main.h"

/**
 * print_numbers - prints number between 0 to 9.
 *
 * Return: Always 0 (Success)
 */

void print_numbers(void)
{
	int z = 48;

	for (z = 48; z < 58; z++)
	{
		_putchar(z);
	}
	_putchar('\n');
}
